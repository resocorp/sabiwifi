[Unit]
# Reconcile WireGuard peers from DB before the app starts serving, so
# the kernel peer set reflects the Router table on every boot/restart.
Wants=sabiwifi-reconcile-wg.service
After=sabiwifi-reconcile-wg.service
